# Dependencies
node_modules/
mapper-frontend/node_modules/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# Environment (all .env variants — never commit secrets)
.env
.env.*
*.env

# Build outputs
dist/
.next/
build/

# Data (local user data)
mapper-backend/data/

# Export output & runtime artifacts
mapper-backend/export/
mapper-backend/unlinked.log
*.log

# Secrets & user data
premise_key
waitlist.json

# ecoinvent archives (licensed — never commit)
*.7z

# Reference codebases (not part of MApper)
_reference/

# Private context notes — never publish (also kept outside the repo tree)
C4MApper/

# Claude Code local config
.claude/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Tauri sidecar binary (huge PyInstaller freeze — never commit)
mapper-tauri/binaries/
# Onedir sidecar staged as a Tauri resource (785 MB PyInstaller freeze — never commit)
mapper-tauri/resources/
mapper-tauri/target/
mapper-tauri/gen/
