# MApper build — 2026-09-01

v0.2.0 was never tagged, and the bump landed ~20 PRs ago. The About panel,
/openapi.json and Info.plist all read 0.2.0 for every build since, so the
version string cannot answer "is the installed app current?". THE COMMIT CAN.

| | |
|---|---|
| **main tip commit** | `3dcca2882ad5ecc59851a17838bebe80f942bd26` |
| short | `3dcca28` |
| subject | Record the premise superstructure fallback; no opt-in flag, and why (#80) |
| committed | 2026-09-01T17:11:40Z |
| tree clean at build | yes |
| version (pyproject / package.json / Info.plist) | 0.2.0 |

## Artefacts

| | |
|---|---|
| SPA bundle | `index-Droml15t.js` |
| SPA sha256 | `2f29c273af3dace0689a32e208a80fdac0c4159f41b3e22c3c1696a11ea56582` |
| DMG | `MApper_0.2.0_aarch64.dmg` |
| DMG bytes | 442164894 |
| DMG sha256 | `8e55dbc2b2fd3910dbf362537bc45e85df56be9605d385ca728cdc504583c536` |

## Origin checkpoints — :8765 grepped at all four stages

| stage | 8765 | 8000 |
|---|---|---|
| 1. source (`apiOriginForMode('desktop')`, executed) | http://localhost:8765 | — |
| 2. built `dist/` | 1 | 0 |
| 3. frozen onedir | 1 | 0 |
| 4. bundled `.app` | 1 | 0 |

`apiOriginForMode('production')` returns :8000 — a bundle built with plain
`npm run build` would succeed and ship pointing at the dev port. That is why
the grep is repeated rather than trusted once.

## Sequence

- `__pycache__` cleared FIRST (7 dirs removed) — stale bytecode survives a
  version bump because the source size is unchanged.
- PyInstaller onedir; UMFPACK bundled (1 extension + 10 SuiteSparse libs).
- Staged with `cp -RL`; 0 symlinks remaining.
- Installed over /Applications; **only** `~/Library/Caches` cleared.
  `~/Library/WebKit` (localStorage — chart colours, onboarding flag) preserved.
- Launched via `open`.

## Verified on the frozen build

- `/openapi.json` → 0.2.0; SPA served same-origin.
- Both Monte Carlo POSTs return a **task_id**, not a 500.
- MC result carries `computed_at`, `mapper_version`, `stage_amounts`,
  `basis_amounts`, `use_phase_basis`, `bom_hashes`, `parameter_table_hash`.
- Export "Calculated" row = compute time (17:18 UTC) while now() was 17:19.
- Paired multi-item MC with a scored parameter reports foreground variance
  (GSD² 1.21 / 1.20; corr 0.9969). Run in a THROWAWAY project, deleted after;
  Battery Circularity's table remains 0 of 68 scored.
