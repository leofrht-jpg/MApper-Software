# Build log — v0.2.1 (macOS arm64)

- **Tip commit:** `a7e478336830344637dc7872b1fb6ec3edb4dd4d`
- **Built:** 2026-09-03T17:53:24Z
- **Host:** macOS 26.6.2, Apple Silicon
- **Target:** `aarch64-apple-darwin`

## Artifact

| | |
|---|---|
| DMG | `MApper_0.2.1_aarch64.dmg` |
| Bytes | `442168868` |
| SHA-256 | `a8df07f916ecfecf63743f1a317d251fac8b140ceb36acb3c93ca51f725bd2fd` |

## SPA hash — identical at every stage

`dist/index.html` sha256 `fc15d596919ce2a6fe55d874c65de80d5985d4e384b43eee4a5f92434424da04`

Verified byte-identical in the frontend build, the frozen sidecar, and the
installed `/Applications/MApper.app`. Asset bundle `index-DYMDEXEc.js`.

## Four-step build

`__pycache__` cleared FIRST (0 dirs remaining). Not optional: a `.pyc` is
treated as fresh when the source's (mtime, size) match, and 0.2.0 -> 0.2.1
leaves the size identical — the exact shape that shipped a stale version
literal on 2026-08-29.

| stage | `:8765` | `:8000` |
|---|---|---|
| 1. frontend `dist/` | present | **0** |
| 2. frozen sidecar | present | 4 — all in botocore's vendored AWS endpoint data (DynamoDB Local), unrelated to MApper's origin |
| 3. staged Tauri resource | present | — (0 symlinks after `cp -RL`, from 53) |
| 4. installed `.app` | present | — |

PyInstaller: **0 errors**. UMFPACK bundled (1 extension + 10 SuiteSparse
native libs). Cargo release build finished in 32s; 2 bundles produced.

## Frozen-binary verification

- `/openapi.json` → **`0.2.1`** (the frozen build, not the dev tree)
- **one** `mapper-backend` process
- `POST /api/lca/monte-carlo` → **200**, task id (not a 500)
- `POST /api/lca/monte-carlo/multi` → **200**, task id (not a 500)
- both tasks ran to completion: single GSD² 1.2441, median 11530.7; paired 1 item
- result carries `computed_at`, `mapper_version=0.2.1`, `parameter_table_hash`,
  `bom_hashes`, `data_fingerprint`, `use_phase_basis=one_year`

## Install

Over `/Applications`; `~/Library/Caches/com.leonardoferhati.mapper` cleared;
`~/Library/WebKit/com.leonardoferhati.mapper` **preserved** (it is
localStorage, not a cache). Quarantine xattr cleared (unsigned build).

## Windows

Not built. `.github/workflows/` contains only `ci.yml`, whose jobs are tests;
there is no release workflow. PyInstaller does not cross-compile and Tauri's
NSIS/MSI bundling requires Windows, so no Windows artifact can be produced
from this host. See the release thread.
