# Build log — 0.3.0 @ `beaeda3` (macOS arm64)

**Release candidate, not a release.** No tag, no GitHub release — walked by hand
first. Supersedes the `d15fe60` build of the same version
(`2026-09-19_0.3.0_d15fe60.md`): same number, now built from `main` after
#142 (per-indicator coverage declaration) and #141 (the bump) merged in that
order. The two are told apart by tip commit.

| | |
|---|---|
| Date | 2026-09-19 |
| Tip commit | `beaeda3` — "Release v0.3.0 (#141)", on `487d3f2` (#142) |
| Version | **0.3.0** (10 sites, unchanged from the `d15fe60` log) |
| DMG | `MApper_0.3.0_aarch64.dmg` |
| DMG bytes | 442,261,446 |
| DMG SHA-256 | `580283eb9b792c6375b60cdee1c3e0842ff50666823e1bf7bd8a425839a531ac` |
| SPA bundle | `index-Doh6Cv8p.js` |
| SPA SHA-256 | `2cffcdd2e3c4aa33a283a4f59ede2cf4b4315589bfd309a66de39d0c5685005b` |
| `index.html` SHA-256 | `ee595f39da0bb6305e034c42bd3b272e5a32afc1123ffd6a1b076f86c9f1b297` |
| Backend suite | 1706 passed (on #142's head; pyflakes guard ran) |
| Frontend suite | 1460 passed; `tsc -b` clean |

## What changed since `d15fe60`

Only #142: `complete_indicators` on partial authored activities, the
`reached-indicators` route, `kind` on coverage gaps, the editor's declaration
step, and a `Why` column on the Coverage sheet. Compute of LCA scores is
untouched; the fingerprint of existing authored databases is unchanged by
design.

## Build gates

- `__pycache__`: 0 before the clear, cleared again before the freeze.
- Port `8765`, matching lines: **1 / 250 / 251 / 251** (frontend / freeze /
  staged resource / bundle) — identical to the previous build. Symlinks in the
  staged resource: **0**.
- SPA hash identical at stage 1, inside the freeze, inside the bundle, and after
  `beforeBuildCommand`'s rebuild.

## Frozen-binary verification (sidecar direct, `MAPPER_PORT=8799`)

- up in 13 s; `/openapi.json` → **0.3.0**; `/api/authored-databases/reached-indicators` present
- `index.html` → `cache-control: no-cache`
- `POST /api/lca/monte-carlo` → `task_id` ✓; `POST /api/lca/monte-carlo/multi` → `task_id` ✓
  (2 items × 2 methods, 2 differences, both completed)
- ICEV-Petrol stock GWP100 deterministic **3195.1647814922817** — unchanged

## Installed app (`open`)

- `/openapi.json` → 0.3.0; bundled SPA `2cffcdd2…`
- Single-product PROSPECTIVE (the `_current_project` fix) still works: ICEV-Petrol
  GWP100 base **3195.1647814922817**, premise SSP2-PkBudg1150 2030
  **3169.7541453929443**, 0 warnings.
- `open -a` returned exit status 1 on this machine at both installs this session
  while launching the app normally; recorded so it is not read as a failed
  launch.

### The flare worked example, corrected through the installed app

v014-demo-check, "Flaring (worked example)" → "Methane flaring, 98 % destruction
efficiency" (partial; CO2 + unburnt CH4):

- `reached-indicators` for its two flows, EF v3.1: 7 indicators.
- Declared complete: **climate change** and **climate change: fossil** (both
  GWP100) — reached by the same two flows and equally complete. Re-saved via the
  editor's `PUT`; code kept (the BOM link survives), exchanges byte-identical,
  materialisation `in_sync`.
- Recomputed, all 25 EF v3.1 indicators:

| | count | indicators |
|---|---|---|
| specified | 2 | climate change, climate change: fossil — 3.2849 kg CO2-eq |
| not specified, **not declared** | 5 | ecotoxicity: freshwater (+ organics), human toxicity: non-carcinogenic (+ organics), photochemical oxidant formation |
| not specified, **not reached** | 18 | acidification, eutrophication ×3, particulate matter, … |

  The five "not declared" are the indicators the old rule reported as
  specified. Scores unchanged.
- Export: `C4MApper/Outputs/Associated-gas_flare_(worked_example)_LCA_v2.xlsx` —
  headline "23 of 25 indicator(s) NOT SPECIFIED", `Why` column populated.

## Install

Over `/Applications`, clearing **only** `~/Library/Caches/com.leonardoferhati.mapper`.
`~/Library/WebKit/com.leonardoferhati.mapper` **404K before and 404K after**.
