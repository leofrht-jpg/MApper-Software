# Build log — 0.3.0 @ `fa47c1b` (macOS arm64)

**Release candidate, not a release.** No tag, no GitHub release. Fourth 0.3.0
build; the version was already 0.3.0 and was **not** bumped.

| | |
|---|---|
| Date | 2026-09-20 |
| Tip commit | `fa47c1b` — "Give every archetype node an id on load, not only on create (#146)" |
| Version | **0.3.0** (unchanged) |
| DMG | `MApper_0.3.0_aarch64.dmg` |
| DMG bytes | 442,276,373 |
| DMG SHA-256 | `7f49dad699085f4aefb3fa75ad4081d2473ff7e8596818d0f64ef609ff3776c5` |
| SPA bundle | `index-BN2Uayof.js` |
| SPA SHA-256 | `640bdcf3227fa5808503e02eaccb3ca562613d915117f3b96d504bfab243b9d4` |
| `index.html` SHA-256 | `dfb94ba0eb4f5c760b6fbe75b0c2234452fb37538699803a1e555222bc30b212` |
| Backend suite | 1730 passed |

Previous build `99df3fa`: 442,275,001 bytes — 1,372 larger here.

**The SPA is byte-identical to the previous build** (same filename, same hash):
#146 is backend-only, and the frontend build reproduced exactly.

## What this build adds over `99df3fa`

#146 only: every archetype node gets an id **on load**, not only on create.

## `__pycache__` cleared FIRST

3 directories present, cleared to 0 before the freeze.

## Port grep — all four stages (matching LINES)

| stage | `8765` lines |
|---|---|
| 1 frontend `dist/` | 1 (the baked origin) |
| 2 frozen `dist/mapper-backend` | 240 (49 files) |
| 3 staged tauri resource | 240 (symlinks: **0**) |
| 4 bundled `MApper.app` | 240 |

Identical to `99df3fa` at every stage; SPA hash identical at stage 1, in the
freeze and in the bundle.

## Frozen-binary verification (sidecar direct, `MAPPER_PORT=8799`)

- up in 14 s; `/api/health` ok, brightway2 3.6.6; `/openapi.json` → `0.3.0`;
  `index.html` → 200; 7 projects
- **`GET /api/bom/archetypes` in the demo project → 200, 2 archetypes, every
  `stage_ids` value populated.** It returned 500 on every build up to and
  including `99df3fa`.
- DEMO combustion vehicle, stock, GWP100 → **2789.9999141693115**, unchanged
- `POST /api/lca/monte-carlo` → task ✓, completed in 2 s
- `POST /api/lca/monte-carlo/multi` → task ✓, completed, 2 items × 2 methods

## The root-id repair, on the real data

The verification run performed the one-time migration on this machine's
storage. All 4 affected archetypes now carry root ids: the demo pair and the
same pair in `MApper demo (synthetic data).bak-174958`. The worked-example
archetypes **kept their existing ids** (`8547eea0…`, `b14ad947…` unchanged) —
the repair is idempotent, proved on the real files rather than only a fixture.

Survey of this machine before the fix: **4 of 73 archetypes** across 7 projects
had null root ids, all four being the demo pair and its backup copy. Battery
Circularity (8), MAp-test (28), Wind Farm (2), default (29) and the
worked-example project (2) were clean — all created through the create route.

## Install

Over `/Applications`, clearing **only** `~/Library/Caches/com.leonardoferhati.mapper`.
`~/Library/WebKit/com.leonardoferhati.mapper` measured **368K before and 368K
after**.

## Project rename: `v014-demo-check` → `v030-demo-check`

Through `POST /projects/rename` (copy-then-delete; `duplicate_project` carries
the bw2 directory and every MApper storage root with ids verbatim), not on disk.

Restore point taken first:
`~/MApper-restore-points/v014-demo-check_BEFORE-rename_20260920-142323`
— 152 MB, 1,567 files, the full Brightway3 project directory plus `dsm/`
(`archetypes/` and `authored_databases.json`).

Verified after the rename: 7 projects before and after, old name gone; both
authored databases load `in_sync`; both archetypes list; **both examples compute
identical scores, identical markers and identical bases**; both BOM links
resolve to their original `(database, code)`. On disk,
`dsm/v030-demo-check` holds both files, `dsm/v014-demo-check` is gone, and the
Brightway directory is the new name hash.

No stale reference to the old name existed anywhere: 0 hits in the repo, in the
project's C4 folder, in the lab's operating manual, or in the project memory.
The delivered workbooks carry no project name.

## Both worked examples, through the installed binary

| | flare | polycarbonate PCF |
|---|---|---|
| basis | `ecoinvent`, `ecoinvent` | **`supplied`** |
| floor status | `unfloored`, `unfloored` | **`not_applicable`** |
| `has_unfloored_exchange` | true | false |
| declared complete | climate change + climate change: fossil | climate change |
| climate change | 3.2848999653160575 | 2.8 |
| markers | 2 specified, 5 not declared, 18 not reached | 1 specified, 1 not declared, 23 not reached |
| GSD² reported | 1.1726, 2.0634 (ecoinvent) | 1.2295 (supplied) |

The two sit in one project and differ in exactly the way the basis is meant to
capture: the flare's amounts are emissions whose floor could not be checked
(no ecoinvent in this project), the PCF's amount is a characterised result the
floor does not apply to.
