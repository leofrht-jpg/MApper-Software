# Build log — 0.3.0 @ `61d4b77` (macOS arm64)

**Release candidate, not a release.** No tag, no GitHub release. Fifth 0.3.0
build; the version was already 0.3.0 and was **not** bumped.

| | |
|---|---|
| Date | 2026-09-21 |
| Tip commit | `61d4b77` — "Pass the project basis to the Multi-item lifetime preset (#149)" |
| Version | **0.3.0** (unchanged) |
| DMG | `MApper_0.3.0_aarch64.dmg` |
| DMG bytes | 442,281,279 |
| DMG SHA-256 | `fe656c057e11e8c31482dd2c10081be8559eeb38b0e0bdf458650bb93f3fa40b` |
| SPA bundle | `index-Bv3RAylR.js` |
| SPA SHA-256 | `e64bc21126797da1d52e740ce34e287c6c450b980415cbc424f54c773a354964` |
| `index.html` SHA-256 | `a958fef151f5140e150d5787b9ae4b53ac63860581b7da406ef6971f25e9c3ec` |
| Backend suite | 1744 passed |
| Frontend suite | 1469 passed; `tsc -b` clean |

Previous build `fa47c1b`: 442,276,373 bytes — 4,906 larger here. The SPA hash
moved for the first time in three builds: #148 and #149 both change frontend
code, where #146 did not.

## What this build adds over `fa47c1b`

- **#148** — results keyed by the method TUPLE; the label is display only.
- **#149** — the Multi-item lifetime preset receives the project basis.

## `__pycache__` cleared FIRST

0 directories present (the suite runs with `PYTHONDONTWRITEBYTECODE=1`),
cleared again before the freeze.

## Port grep — all four stages (matching LINES)

| stage | `8765` lines |
|---|---|
| 1 frontend `dist/` | 1 (the baked origin) |
| 2 frozen `dist/mapper-backend` | 240 (49 files) |
| 3 staged tauri resource | 240 (symlinks: **0**) |
| 4 bundled `MApper.app` | 240 |

SPA hash identical at stage 1, inside the freeze and inside the bundle.

## Frozen-binary verification (sidecar direct, `MAPPER_PORT=8799`)

- up in 13 s; `/api/health` ok, brightway2 3.6.6; `/openapi.json` → `0.3.0`;
  `index.html` → 200; 7 projects
- `GET /api/bom/archetypes` in the demo project → 200, 2 archetypes, every
  `stage_ids` populated (the #146 repair, still holding)
- DEMO combustion vehicle, stock, GWP100 → **2789.9999141693115**, unchanged
  across five builds
- `POST /api/lca/monte-carlo` → task ✓, completed in 2 s
- `POST /api/lca/monte-carlo/multi` → task ✓, completed, 2 items × 2 methods

## The collapse is gone, measured on a real run

Through the installed binary, in `v030-demo-check`, over all 25 EF v3.1
indicators:

| | flare | polycarbonate |
|---|---|---|
| `stage_breakdown` entries | **25** | **25** |
| distinct method tuples | **25** | **25** |
| distinct `method[-1]` labels | 14 | 14 |
| climate change | 3.2848999653160575 | 2.8 |

The label collision is still present in the data — 14 distinct indicator names
for 25 indicators — and no longer collapses anything. The re-exported
workbooks carry **25 rows with 25 distinct names**, and all four climate
variants appear under their own names beside their own method paths:

```
climate change                                 EF v3.1 › climate change › global warming potential (GWP100)
climate change: biogenic                       EF v3.1 › climate change: biogenic › …
climate change: fossil                         EF v3.1 › climate change: fossil › …
climate change: land use and land use change   EF v3.1 › climate change: land use and land use change › …
```

Against the pre-fix exports: 14 rows, and the row named "global warming
potential (GWP100)" was *climate change: land use and land use change*.

## Re-exported worked examples

`Associated-gas_flare_(worked_example)_LCA_v3.xlsx` and
`Enclosure,_75_%_recycled_polycarbonate_(worked_example)_LCA_v3.xlsx` in
`C4MApper/Outputs`. The `_v2` / `_supplied-basis` files are superseded: their
Stage breakdown sheets hold 14 of 25 rows. Their `Total impacts` sheets were
always correct — written positionally — so any headline figure taken from
there still stands.

## Install

Over `/Applications`, clearing **only** `~/Library/Caches/com.leonardoferhati.mapper`.
`~/Library/WebKit/com.leonardoferhati.mapper` measured **504K before and 504K
after**.
