{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "MApper desktop capabilities: show error dialogs. The backend onedir sidecar is spawned via std::process::Command from the bundled resource dir (not the shell plugin), so no shell capability is needed.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
